apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: db-prayer
    app.kubernetes.io/managed-by: Helm
    cattle.io/creator: norman
    objectset.rio.cattle.io/hash: dbb408b909497414f93be5e7a78a2f285d27b7e1
    workload.user.cattle.io/workloadselector: statefulSet-default-db-prayer
  name: db-prayer
spec:
  podManagementPolicy: OrderedReady
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: statefulSet-default-db-prayer
  serviceName: db-prayer
  template:
    metadata:
      annotations:
        cattle.io/timestamp: "2022-09-04T02:49:36Z"
        field.cattle.io/ports: '[[]]'
      creationTimestamp: null
      labels:
        workload.user.cattle.io/workloadselector: statefulSet-default-db-prayer
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: stack
                operator: In
                values:
                - odoo
      containers:
      - args:
        - --max_connections=100
        - --shared_buffers=3072MB
        - --work_mem=3GB
        - --dynamic_shared_memory_type=posix
        - --maintenance_work_mem=2GB
        - --max_wal_size=8GB
        - --min_wal_size=4GB
        - --checkpoint_completion_target=0.9
        - --effective_cache_size=45GB
        - --max_parallel_workers_per_gather=8
        env:
        - name: POSTGRES_DB
          value: postgres
        - name: POSTGRES_PASSWORD
          value: odoo
        - name: POSTGRES_USER
          value: odoo
        image: postgres:15
        imagePullPolicy: IfNotPresent
        name: db-prayer
        
        resources:
          requests:
            cpu: 100m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities: {}
          privileged: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
        stdin: true
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        tty: true
        volumeMounts:
        # Edit the volume to match the existing volumeMounts in the cluster
        - mountPath: /dev/shm
          name: dshm
        - mountPath: /var/lib/postgresql/data
          name: vol-ytjxx
          subPath: database
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      volumes:
      - emptyDir:
          medium: Memory
          sizeLimit: 4Gi
        name: dshm
      # Edit the volume to match the existing pv in the cluster
      - name: vol-ytjxx
        persistentVolumeClaim:
          claimName: odoo-prayer
  updateStrategy:
    type: RollingUpdate
status:
  availableReplicas: 1
  replicas: 1
