apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-sfca
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-sfca.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-sfca
            port:
              name: http
  tls:
  - hosts:
    - odoo-sfca.webdiscode.com
    secretName: odoo-sfca.webdiscode.com-tls
