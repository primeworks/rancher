apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-tbvng
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-tbvng.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-tbvng
            port:
              name: http
  tls:
  - hosts:
    - odoo-tbvng.webdiscode.com
    secretName: odoo-tbvng.webdiscode.com-tls
