apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-dezainca
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-dezainca.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-dezainca
            port:
              name: http
  tls:
  - hosts:
    - odoo-dezainca.webdiscode.com
    secretName: odoo-dezainca.webdiscode.com-tls
