apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-sflow
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-sflow.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-sflow
            port:
              name: http
  - host: silverflow.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-sflow
            port:
              name: http
  tls:
  - hosts:
    - odoo-sflow.webdiscode.com
    secretName: odoo-sflow.webdiscode.com-tls
  - hosts:
    - silverflow.webdiscode.com
    secretName: silverflow.webdiscode.com-tls
