apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-prayer
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: prayer.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-prayer
            port:
              name: http
  tls:
  - hosts:
    - prayer.webdiscode.com
    secretName: prayer.webdiscode.com-tls
