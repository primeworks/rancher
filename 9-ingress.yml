apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-didisng
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-didisng.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-didisng
            port:
              name: http
  tls:
  - hosts:
    - odoo-didisng.webdiscode.com
    secretName: odoo-didisng.webdiscode.com-tls
