apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-17-b
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-17-b.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-17-b
            port:
              name: http
  tls:
  - hosts:
    - odoo-17-b.webdiscode.com
    secretName: odoo-17-b.webdiscode.com-tls
