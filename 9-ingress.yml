apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-alpha
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: alpha.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-alpha
            port:
              name: http
  tls:
  - hosts:
    - alpha.webdiscode.com
    secretName: alpha.webdiscode.com-tls
