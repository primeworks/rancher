apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-fasterscale
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-fasterscale.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-fasterscale
            port:
              number: 8069