apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-bukka-stag
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: bukka-stag.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-bukka-stag
            port:
              number: 8069