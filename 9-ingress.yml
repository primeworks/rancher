apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-tbv
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: tbv.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-dezain
            port:
              number: 8069