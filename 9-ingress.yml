apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-cnet3
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: cnet3.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-cnet3
            port:
              number: 8069
