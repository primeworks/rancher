apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-valuec
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-valuec.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-valuec
            port:
              name: http
  tls:
  - hosts:
    - odoo-valuec.webdiscode.com
    secretName: odoo-valuec.webdiscode.com-tls
