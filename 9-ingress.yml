apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-lighthouse
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: lighthouse.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-lighthouse
            port:
              number: 8069