apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-expired
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-expired.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-expired
            port:
              name: http
  tls:
  - hosts:
    - odoo-expired.webdiscode.com
    secretName: odoo-expired.webdiscode.com-tls
