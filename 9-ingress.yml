apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-hh
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-hh.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-hh
            port:
              name: http
  tls:
  - hosts:
    - odoo-hh.webdiscode.com
    secretName: odoo-hh.webdiscode.com-tls
