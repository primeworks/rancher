apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-didis
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: didis.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-didis
            port:
              number: 8069