apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-ssca
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: ssca.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-ssca
            port:
              number: 8069