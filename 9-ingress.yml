apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-eliteboxng
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-eliteboxng.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-eliteboxng
            port:
              name: http
  tls:
  - hosts:
    - odoo-eliteboxng.webdiscode.com
    secretName: odoo-eliteboxng.webdiscode.com-tls
