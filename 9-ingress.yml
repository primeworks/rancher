apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-evoucherng
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-evoucherng.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-evoucherng
            port:
              name: http
  tls:
  - hosts:
    - odoo-evoucherng.webdiscode.com
    secretName: odoo-evoucherng.webdiscode.com-tls
