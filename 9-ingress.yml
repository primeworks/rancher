apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-silverflowca
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-silverflowca.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-silverflowca
            port:
              name: http
  tls:
  - hosts:
    - odoo-silverflowca.webdiscode.com
    secretName: odoo-silverflowca.webdiscode.com-tls
