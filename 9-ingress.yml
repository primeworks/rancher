apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-evoucher
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-evoucher.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-evoucher
            port:
              number: 8069