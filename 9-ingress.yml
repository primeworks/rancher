apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-flavor
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-flavor.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-flavor
            port:
              name: http
  tls:
  - hosts:
    - odoo-flavor.webdiscode.com
    secretName: odoo-flavor.webdiscode.com-tls
