apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-silver
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-silver.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-silver
            port:
              name: http
  tls:
  - hosts:
    - odoo-silver.webdiscode.com
    secretName: odoo-silver.webdiscode.com-tls
