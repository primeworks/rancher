apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-eliteboxng
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: eliteboxng.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-eliteboxng
            port:
              number: 8069