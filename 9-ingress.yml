apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-fbds
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: fbds.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-fbds
            port:
              number: 8069