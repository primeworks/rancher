apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-cbpei
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-cbpei.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-cbpei
            port:
              name: http
  tls:
  - hosts:
    - odoo-cbpei.webdiscode.com
    secretName: odoo-cbpei.webdiscode.com-tls
