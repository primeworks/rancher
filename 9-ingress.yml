apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-17-demo
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-17-demo.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-17-demo
            port:
              name: http
  tls:
  - hosts:
    - odoo-17-demo.webdiscode.com
    secretName: odoo-17-demo.webdiscode.com-tls
