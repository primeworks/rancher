apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-sf
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-sf.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-sf
            port:
              name: http
  tls:
  - hosts:
    - odoo-sf.webdiscode.com
    secretName: odoo-sf.webdiscode.com-tls
