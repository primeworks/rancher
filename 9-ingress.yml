apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-abopeng
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-abopeng.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-abopeng
            port:
              name: http
  tls:
  - hosts:
    - odoo-abopeng.webdiscode.com
    secretName: odoo-abopeng.webdiscode.com-tls
