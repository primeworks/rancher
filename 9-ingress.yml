apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-cbf
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-cbf.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-cbf
            port:
              name: http
  tls:
  - hosts:
    - odoo-cbf.webdiscode.com
    secretName: odoo-cbf.webdiscode.com-tls
