apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-s24
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: s24.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-s24
            port:
              number: 8069