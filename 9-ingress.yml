apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-helptest
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: helptest.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-helptest
            port:
              number: 8069