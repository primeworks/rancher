apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-fishing
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: fishing.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-fishing
            port:
              name: http
  tls:
  - hosts:
    - fishing.webdiscode.com
    secretName: fishing.webdiscode.com-tls
