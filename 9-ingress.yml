apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-dezain
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-dezain.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-dezain
            port:
              name: http
  tls:
  - hosts:
    - odoo-dezain.webdiscode.com
    secretName: odoo-dezain.webdiscode.com-tls
