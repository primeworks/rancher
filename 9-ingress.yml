apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-silverflow
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: odoo-silverflow.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-silverflow
            port:
              number: 8069