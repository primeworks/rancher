apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-setfree
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: setfree.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: odoo-setfree
            port:
              name: http
  tls:
  - hosts:
    - setfree.webdiscode.com
    secretName: setfree.webdiscode.com-tls
