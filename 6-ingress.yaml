apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: splunk-sfxt
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: sfxt.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: splunk-sfxt
            port:
              number: 8069
