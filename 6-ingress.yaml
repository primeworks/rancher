apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sswss-odoo
  namespace: sswss-odoo
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: sswss-odoo.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sswss-odoo
            port:
              number: 8069
