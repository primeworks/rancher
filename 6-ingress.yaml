apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: splunk-basic
  annotations:
    cert-manager.io/issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: splunk-basic.webdiscode.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: splunk-basic
            port:
              name: http
  tls:
  - hosts:
    - splunk-basic.webdiscode.com
    secretName: splunk-basic.webdiscode.com-tls
