apiVersion: v1
kind: Service
metadata:
  name: odoo-bukka
spec:
  selector:
    workload.user.cattle.io/workloadselector: deployment-default-odoo-bukka
  ports:
    - protocol: TCP
      targetPort: odoo-bukka
      port: 8069
      name: odoo-bukka