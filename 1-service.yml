apiVersion: v1
kind: Service
metadata:
  name: odoo-s24
spec:
  selector:
    workload.user.cattle.io/workloadselector: deployment-default-odoo-s24
  ports:
    - protocol: TCP
      targetPort: odoo-s24
      port: 8069
      name: odoo-s24