apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: odoo-plastic-app
  annotations:
    # cert-manager.io/issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  # ingressClassName: nginx
  rules:
  - host: odoo-plastic.webdiscode.com
    http:
      paths:
      - path: /web#(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: odoo-plastic-app
            port:
              name: http
  # - host: www.plasticocbpn.ca
  #   http:
  #     paths:
  #     - path: /web#(/|$)(.*)
  #       pathType: Prefix
  #       backend:
  #         service:
  #           name: odoo-plastic-app
  #           port:
  #             name: http
  tls:
  - hosts:
    - odoo-plastic.webdiscode.com
    secretName: odoo-plastic.webdiscode.com-tls
  # - hosts:
  #   - www.plasticocbpn.ca
  #   secretName: www.plasticocbpn.ca-tls
