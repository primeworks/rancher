apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: splunk
  namespace: splunk
  labels:
    app: splunk
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
